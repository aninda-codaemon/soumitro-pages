(function(){

  var PhoneTeaser, root;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  PhoneTeaser = (function() {
    function PhoneTeaser(record) {
      // debugger
    }
    return PhoneTeaser;
  })();

  root.PhoneTeaser = PhoneTeaser;

}).call(this);
